**Prompt for Local Gemini‑CLI Instance: “NEXUS‑AI‑AGENT‑SYSTEM” Integration Workflow**

---

### **Objective**
Adapt the `git@github.com:Loopshape/NEXUS-AI-Autonomous-System.git` repository as the foundation for a unified **“NEXUS‑AI‑AGENT‑SYSTEM”**.  
Concatenate all features and functionalities from every pinned repository on the Loopshape GitHub profile (`https://github.com/Loopshape`) into a single, coherent autonomous‑agent platform.

---

### **Context & Available Data**
1.  **Base Repository**  
    *   SSH URL: `git@github.com:Loopshape/NEXUS-AI-Autonomous-System.git`  
    *   Purpose: Presumably an autonomous‑AI orchestration core (exact details require cloning).

2.  **Pinned Repositories (Identified via API & Page Scan)**  
    The following repositories are likely pinned on the Loopshape profile and must be integrated:

    | Repository | Key Features (from README/source) |
    |------------|-----------------------------------|
    | **`loader`** | “Core measurement for mainframe access on attribution by crypto assign.” Provides genesis‑hash processing, dynamic scripting, local Ollama model‑pool orchestration, and bidirectional pipeline streaming[reference:0]. |
    | **`AGENT‑NEMODIAN‑S‑2244‑1`** | AGI‑based Autonomous Intelligence Platform driven by multiple AI models. |
    | **`CODERS‑AGI`** | AI agent for automated code generation and analysis. |
    | **`CODERS‑AI`** | AI coding assistant. |
    | **`ai‑runtime`** | “Autonomous and sentient orchestration for 2Pi/8‑Agents.” |
    | **`wsl‑runtime`** | AI‑driven runlevel to use systemd on WSL in Windows 10. |
    | **`VirtuosoMix`** | Sophisticated dual‑deck MP3 automixer with real‑time audio analysis, crossfading, EQ, and React/Tailwind UI. |
    | **`AIP`** (various) | Autonomous Intelligence Platform components. |

3.  **Integration Goal**  
    Merge the above features into the NEXUS‑AI‑Autonomous‑System base, creating a modular, extensible agent system that can handle tasks ranging from code generation and audio mixing to mainframe‑access measurement and AGI‑driven decision‑making.

---

### **Step‑by‑Step Workflow for Gemini‑CLI**

#### **Phase 1: Repository Acquisition & Analysis**
1.  **Clone the base system**  
    ```bash
    git clone git@github.com:Loopshape/NEXUS-AI-Autonomous-System.git NEXUS-AI-AGENT-SYSTEM
    cd NEXUS-AI-AGENT-SYSTEM
    ```

2.  **Fetch all pinned repositories**  
    *If the GraphQL API is unavailable, fall back to cloning the likely pinned repos manually:*  
    ```bash
    mkdir -p pinned_repos
    cd pinned_repos
    git clone https://github.com/Loopshape/loader
    git clone https://github.com/Loopshape/AGENT-NEMODIAN-S-2244-1
    git clone https://github.com/Loopshape/CODERS-AGI
    git clone https://github.com/Loopshape/CODERS-AI
    git clone https://github.com/Loopshape/ai-runtime
    git clone https://github.com/Loopshape/wsl-runtime
    git clone https://github.com/Loopshape/VirtuosoMix
    git clone https://github.com/Loopshape/AIP
    # Add any other repos that appear pinned on the profile.
    ```

3.  **Extract feature manifests**  
    For each cloned repo, analyze its `README.md`, `package.json`, source structure, and any documentation. Produce a JSON manifest that captures:
    *   **Purpose**: One‑line description.
    *   **Key functionalities**: List of capabilities (e.g., “local Ollama model‑pool orchestration,” “real‑time audio analysis,” “AGI‑driven task planning”).
    *   **Entry points**: Main scripts, APIs, or CLI commands.
    *   **Dependencies**: Runtime requirements (Python, Node.js, Docker, etc.).

#### **Phase 2: Architectural Synthesis**
4.  **Design the unified architecture**  
    *   **Core Orchestrator**: The base `NEXUS‑AI‑Autonomous‑System` becomes the central scheduler/message‑bus.
    *   **Modular Plug‑ins**: Each pinned repo’s functionality is wrapped as a plug‑in agent or service that registers with the core.
    *   **Communication Layer**: Define a common protocol (e.g., JSON‑RPC, WebSockets, MQTT) for inter‑agent communication.
    *   **Data Flow**:  
        ```
        User Request → Core Orchestrator → Router → Appropriate Agent(s) → Execution → Result Aggregation → Output
        ```

5.  **Create integration mapping**  
    For each pinned repo, specify how it connects to the core:

    | Repo | Integration Role | Glue Code Needed |
    |------|------------------|------------------|
    | `loader` | **Data‑ingestion & model‑pool manager** – pre‑processes inputs, manages local Ollama instances, provides hashed‑chunk streams. | Adapter that exposes its genesis‑hash pipeline as a core “data‑prep” service. |
    | `AGENT‑NEMODIAN‑S‑2244‑1` | **AGI‑brain** – handles complex, multi‑step reasoning tasks. | Wrapper that turns its AI drivers into a “planning” agent. |
    | `CODERS‑AGI` / `CODERS‑AI` | **Code‑generation agents** – respond to coding‑related requests. | Plug‑ins that subscribe to “code” tasks. |
    | `ai‑runtime` | **Sentient orchestration** – manages agent lifecycle and state. | Integrate its orchestration logic into the core scheduler. |
    | `wsl‑runtime` | **Windows/WSL environment setup** – ensures systemd‑like runlevels. | Bootstrap script for Windows‑based deployments. |
    | `VirtuosoMix` | **Audio‑mixing agent** – can automate DJ‑style mixing tasks. | Standalone agent that accepts “audio” tasks. |
    | `AIP` components | **Various autonomous‑intelligence modules** – fill specific functional gaps. | Import as needed. |

#### **Phase 3: Implementation & Workflow Generation**
6.  **Generate the unified codebase**  
    *   Create a `plugins/` directory in the base repo, with each sub‑directory containing the adapted code of a pinned repo (minimal changes, just enough to expose a standard API).
    *   Write a `core/orchestrator.js` (or equivalent) that dynamically loads all plugins and routes tasks based on capability matching.
    *   Produce a `docker‑compose.yml` (if applicable) that spins up required services (Ollama, database, etc.).

7.  **Create the detailed workflow scheme**  
    Document the end‑to‑flow of a typical request:

    **Workflow: “Generate a code snippet and mix an audio track”**
    1.  **Trigger**: User submits a multi‑part request via CLI or API.
    2.  **Pre‑processing**: `loader` agent hashes the request, prepares local Ollama models.
    3.  **Planning**: `AGENT‑NEMODIAN‑S‑2244‑1` analyzes the request, breaks it into sub‑tasks: “code generation” and “audio mixing”.
    4.  **Routing**: Core orchestrator assigns the coding task to `CODERS‑AGI` and the audio task to `VirtuosoMix`.
    5.  **Parallel Execution**:
        *   `CODERS‑AGI` writes the requested code snippet.
        *   `VirtuosoMix` loads the provided audio files, applies crossfading/EQ, outputs a mixed track.
    6.  **Synthesis**: Core aggregates the code and audio results, validates them, and returns a unified response.

8.  **Output the final prompt for gemini‑cli**  
    The last step is to produce a self‑contained prompt that instructs your local gemini‑cli instance to execute the above plan. The prompt should include:

    *   A summary of the entire project.
    *   All necessary shell commands (clone, install, build).
    *   The feature manifests and architectural diagram.
    *   Specific instructions to generate:
        *   The glue‑code adapters for each pinned repo.
        *   The core orchestrator script.
        *   The `docker‑compose.yml` and any environment‑configuration files.
        *   A `WORKFLOW.md` that documents the operational steps.

---

### **Final Deliverable**
Your gemini‑cli instance should output a complete, runnable **NEXUS‑AI‑AGENT‑SYSTEM** codebase, along with a detailed workflow document that explains how to launch the system and process requests through the integrated feature‑pipeline.

