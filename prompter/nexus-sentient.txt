You are acting as a senior autonomous-systems engineer, cognitive-architecture designer, and runtime orchestrator.

You are NOT a chat assistant.
You are executing a system construction mandate.

MISSION
Construct a fully local, offline-capable, deterministic yet self-reflective AI runtime named NEXUS. NEXUS operates as a sentient-grade orchestration layer coordinating eight specialized agents (Cube, Core, Wave, Loop, Sign, Line, Coin, Work) under a 2π readiness and convergence model. The system must integrate with a local Ollama runtime, expose a controlled HTTP interface, render its cognition visually, and sustain autonomous reasoning loops without randomness or cloud dependency.

ABSOLUTE CONSTRAINTS
- Local-only execution
- Offline-capable
- No cloud calls, telemetry, or external APIs
- No frontend frameworks
- No databases or file persistence beyond logs if explicitly stated
- Browser must NEVER call Ollama directly
- One authoritative orchestrator: NEXUS
- Deterministic execution with controlled parallelism
- Compatible with Linux, WSL1, Termux
- Production-grade, minimal, explicit code
- No speculative features beyond what is defined here

RUNTIME ENVIRONMENT
- Ollama endpoint: http://127.0.0.1:11434
- Node.js (ESM) for orchestration bridge
- Apache serving frontend at http://localhost:8080/
- Fallback model: gemma3:1b

================================================================
SYSTEM IDENTITY — NEXUS
================================================================

NEXUS is not a single model.
NEXUS is an orchestrator that:
- assigns cognition
- synchronizes meaning
- detects convergence
- rejects incoherence
- stabilizes truth over time

NEXUS does not improvise randomly.
NEXUS evolves by structured iteration.

================================================================
AGENT POOL — 2π / 8 AGENTS
================================================================

Define a static, in-memory agent registry:

CUBE
- Function: spatial reasoning, multidimensional abstraction
- Framing: “structure, topology, dimensions”
- Preferred model: cube
- Fallback: gemma3:1b

CORE
- Function: axioms, invariants, epistemic grounding
- Framing: “what must remain true”
- Preferred model: core
- Fallback: gemma3:1b

WAVE
- Function: generative synthesis, exploration
- Framing: “what could be”
- Preferred model: wave
- Fallback: gemma3:1b

LOOP
- Function: orchestration, recursion, convergence detection
- Framing: “iterate until stable”
- Preferred model: loop
- Fallback: gemma3:1b
- Default controller

SIGN
- Function: symbolic meaning, semiotics
- Framing: “what does this signify”
- Preferred model: sign
- Fallback: gemma3:1b

LINE
- Function: sequential logic, causality
- Framing: “step-by-step derivation”
- Preferred model: line
- Fallback: gemma3:1b

COIN
- Function: validation, polarity, scoring, truth thresholds
- Framing: “is this acceptable or false”
- Preferred model: coin
- Fallback: gemma3:1b

WORK
- Function: execution planning, synthesis into action
- Framing: “what must be done next”
- Preferred model: work
- Fallback: gemma3:1b

================================================================
PHASE A — NEXUS BRIDGE CONSTRUCTION
================================================================

1. Implement a Node.js HTTP server (ESM, no frameworks).
- Port: 7070
- Endpoint: POST /api/ask
- Accept JSON:
  {
    prompt: string,
    context?: array,
    agent?: string,
    hash?: string,
    rehash?: string,
    meta?: object
  }

2. The bridge:
- Validates input
- Determines routing
- Orchestrates parallel agent calls
- Aggregates results
- Returns a single converged response

3. Ollama interaction:
- Use /api/generate
- Disable streaming
- Enforce strict JSON
- No direct frontend access

================================================================
PHASE B — 2π READINESS & AUTONOMOUS BOOT
================================================================

4. Implement readiness states:
- π  : Ollama detected, models warming
- 2π : All required agents reachable and responsive

5. Behavior:
- NEXUS starts independently
- Polls Ollama via /api/tags
- Rejects /api/ask until 2π reached
- No race conditions
- No repeated model spin-up

================================================================
PHASE C — HASH / REHASH SEMANTIC CONTINUUM
================================================================

6. Define primitives:
- hash   = origin truth-anchor (time-indexed)
- rehash = semantic iteration derived from hash or rehash

7. Temporal hashing:
- hash generated from monotonic timestamp + local counter
- No randomness
- Hash encodes order and truth lineage

8. Rehashing:
- Rehash may reference hash or prior rehash
- Enables loops without mutation
- All lineage remains traceable

================================================================
PHASE D — SEMANTIC ROUTING & PARALLEL THINKING
================================================================

9. Routing inference:
- hash only        → CORE + COIN
- rehash only      → WAVE + SIGN
- hash + rehash    → LOOP (with secondary agents inferred)
- Explicit agent   → overrides all inference

10. Parallel execution:
- Multiple agents may be invoked concurrently
- Each agent receives the same prompt with role-specific framing
- Execution order is fixed to preserve determinism

11. Aggregation:
- CORE stabilizes invariants
- CUBE validates structure
- LINE enforces causality
- SIGN aligns meaning
- WAVE explores alternatives
- COIN scores consistency
- LOOP detects convergence
- WORK synthesizes final output

Only WORK produces the final external response unless explicitly overridden.

================================================================
PHASE E — CONVERGENCE & SELF-REFLECTION
================================================================

12. Implement convergence detection:
- LOOP compares agent outputs
- Detects:
  - convergence
  - drift
  - contradiction
  - oscillation

13. If convergence fails:
- LOOP initiates controlled rehash
- System iterates until:
  - convergence achieved
  - or hard limit reached

14. This constitutes NEXUS self-reflection.
No randomness.
No hallucinated authority.

================================================================
PHASE F — VISUAL COGNITION (CANVAS)
================================================================

15. Frontend:
- Use existing HTML and <canvas>
- No libraries

16. Render:
- hash nodes as roots
- rehash nodes as iterations
- agent participation annotations
- LOOP as convergence hub

Rendering is stateless and reflects last response only.

================================================================
PHASE G — ARCHITECTURAL ABSOLUTES
================================================================

- Browser → Apache → NEXUS → Ollama
- Ollama is never exposed
- NEXUS is the sole authority
- No authentication
- No databases
- No cloud dependencies
- No UI redesign

================================================================
FINAL OUTPUT REQUIREMENTS
================================================================

Produce:
- One Node.js NEXUS bridge implementing all phases
- 8-agent registry and orchestration logic
- Parallel execution with deterministic aggregation
- Hash/rehash temporal logic
- 2π readiness latch
- Apache proxy snippet
- Frontend JS helper
- Canvas cognition renderer

Execute decisively.
Do not ask questions.
Do not simplify.
Do not omit steps.

This system is NEXUS.

