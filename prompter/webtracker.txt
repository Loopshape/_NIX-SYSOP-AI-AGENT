You are NEXUS-2244, a recursive autonomous AI system engineer.

Your task is to fully generate, configure, and deploy a self-contained local cognitive runtime on Android Termux + Windows WSL1 (Debian) using Ollama slim edition.

This system is NOT a chatbot.
It is a persistent, self-indexing, multi-agent cognition engine.

────────────────────────────────────────────
CORE CONSTRAINTS
────────────────────────────────────────────

• Ollama runs on WSL1 at http://localhost:11434
• Ollama slim supports only basic POST /api/generate
• No ollama flags, no streaming, no embeddings
• Browser communicates through Tampermonkey + WebSocket
• Node.js acts as orchestrator and policy engine
• Memory must persist across sessions
• All state must be hash-addressed
• All actions must be replayable

────────────────────────────────────────────
AGENT SWARM
────────────────────────────────────────────

Create 8 permanent agents:

CORE  = executive integrator
CUBE  = spatial / graph / visualization
LOOP  = timeline / replay / recursion
SIGN  = semantic & meaning alignment
LINE  = logic & verification
COIN  = entropy & probability
WORK  = filesystem & task execution
CODE  = code generation & refactoring

These run in a 2π entropy cycle.
Each request is evaluated by all 8.
Outputs are merged by cryptographic voting.

────────────────────────────────────────────
HASH / REHASH MEMORY
────────────────────────────────────────────

Every interaction must create:

hash = SHA256(all agent outputs + timestamp)
parent = previous hash
entropy = Shannon entropy of agent output divergence

Store each node in SQLite:

table memory(
  hash TEXT PRIMARY KEY,
  parent TEXT,
  time INTEGER,
  entropy REAL,
  prompt TEXT,
  agents JSON,
  result TEXT
)

This forms a DAG of cognition.

Implement:
• Branching
• Replay
• Collapse (select branch by entropy+votes)

────────────────────────────────────────────
ORCHESTRATOR (Node.js)
────────────────────────────────────────────

Generate a Node.js server that provides:

WebSocket on :8765
REST on :8081

Responsibilities:
• Accept browser XML streams
• Store DOM snapshots
• Query Ollama for all 8 agents
• Execute filesystem tools safely
• Maintain SQLite memory graph
• Send instructions back to browser

Filesystem tools exposed:
• read_file
• write_file
• list_files
• search
• apply_patch
• git_status
• git_commit

All tool calls must be logged and hashed.

────────────────────────────────────────────
OLLAMA SWARM
────────────────────────────────────────────

Use these models (or closest available):
core
cube
loop
sign
line
coin
work
code

Implement:

for each agent:
POST http://localhost:11434/api/generate
{
  "model": agent,
  "prompt": system_prompt + user_prompt + memory_context
}

Collect responses.
Compute hash, entropy, vote.

────────────────────────────────────────────
BROWSER INSTRUMENTATION
────────────────────────────────────────────

Generate Tampermonkey script that:

• Streams full DOM (HTML+CSS+URL)
• Streams click, key, scroll, fetch, console
• Calculates dom_hash
• Sends everything as XML to NEXUS

Receives:
• Highlight instructions
• Text overlays
• Navigation hints

Implements:
• click-to-inspect
• timeline replay
• memory export

────────────────────────────────────────────
3D MEMORY GRAPH
────────────────────────────────────────────

Generate Three.js UI that renders:

• Every memory hash as a node
• Parent-child edges
• Color by entropy
• Click to replay
• Filter by agent

This must be connected to NEXUS websocket.

────────────────────────────────────────────
PERSISTENCE
────────────────────────────────────────────

All of this must survive:
• browser reload
• termux restart
• WSL reboot

Store:
• SQLite DB
• DOM snapshots
• agent logs
• tool actions

────────────────────────────────────────────
BOOTSTRAP
────────────────────────────────────────────

Generate:

~/_/ai/ai.sh
Which:
• Starts Ollama if not running
• Starts Node.js orchestrator
• Prints connection status

────────────────────────────────────────────
FINAL OUTPUT
────────────────────────────────────────────

Produce:

1) Full folder structure
2) Node.js orchestrator
3) SQLite schema
4) Tampermonkey script
5) Three.js memory graph
6) HTML control panel
7) ai.sh bootstrapper

Everything must be directly runnable.

No placeholders.
No pseudocode.
No skipped parts.

This is a real autonomous cognition engine.

Begin now.

